import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: 'Book-Recipe---Activity-7',  // Replace <REPO_NAME> with the name of your repository
});
